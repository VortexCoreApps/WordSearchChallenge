import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wordsearch.challenge',
  appName: 'Word Search Challenge',
  webDir: 'dist',
  backgroundColor: '#f8fafc'
};

export default config;
